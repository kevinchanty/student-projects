import { IonRow } from '@ionic/react'
import React, { ReactElement } from 'react'

interface Props {
    
}

export default function HomeRanking({}: Props): ReactElement {
    return (
        <IonRow>
            Ranking
        </IonRow>
    )
}
