// not in use
import { IonButton, IonButtons } from "@ionic/react";
import React from "react";

export default function AddPlayerBtn() {
    return (
        <IonButtons>
            <IonButton>add new player</IonButton>
        </IonButtons>
    );
}
