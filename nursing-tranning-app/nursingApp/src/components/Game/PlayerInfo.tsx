// not in use
import React from "react";

export default function PlayerInfo() {
    return (
        <>
            player info component all player info should be retrieved from
            server
        </>
    );
}
